# X2 — First end-to-end with the mock worker (H8, 30 min, everyone)

| Track | Type | Est | Target | Status |
|---|---|---|---|---|
| All | HITL | 30m | H8 | todo |

## Script
Dashboard → New Claim → QR → phone scans → walk-around (can be 15 photos around a chair) → completion → dashboard shows `uploading` → `processing` → (mock, ~20s) `ready` → open claim → viewer loads ToyCar → place 2 pins → reload → pins persist → export JSON.

## Acceptance criteria
- [ ] The script runs without anyone touching a terminal
- [ ] Every break found is written as a bullet under "X2 findings" in `docs/BOARD.md` with an owner
- [ ] Screenshots/recordings from this run go to Track C for the deck
