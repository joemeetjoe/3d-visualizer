# C8b — Demo script, fallbacks, rehearsal

| Track | Type | Est | Target | Status |
|---|---|---|---|---|
| C | HITL | 2h | H20 | todo |

## What to build
Finalize `docs/presentation/DEMO_SCRIPT.md` with exact clicks, who holds the phone, timings per beat, and the fallback at each beat (`FALLBACK_PLAN.md`). Run X4 twice: once clean, once deliberately triggering fallbacks (kill the worker, kill the tunnel, phone denies camera). Record the clean run as a video backup.

## Acceptance criteria
- [ ] Clean run ≤ 10 min including Q&A buffer
- [ ] Every fallback exercised once and recovers in < 20s
- [ ] Backup video (screen + phone) saved locally on the demo laptop and a second device
- [ ] Presenter and phone-holder assigned; both have rehearsed twice

## Blocked by
C7b, C8a, X3
