# X1 — Contract freeze (H2, 15 min, everyone)

| Track | Type | Est | Target | Status |
|---|---|---|---|---|
| All | HITL | 15m | H2 | todo |

## Agenda
1. C walks through `docs/CONTRACTS.md` §1–§8 in 5 minutes.
2. Each track raises anything they need changed (field names, one more endpoint, an env var). Decide on the spot.
3. C commits the final `packages/shared-types` and CONTRACTS.md. From now on changes go through C.
4. Confirm: tunnel URL pinned, GPU box IP pinned, daylight time for D2a pinned.

## Acceptance criteria
- [ ] shared-types pushed and imported by web + api
- [ ] Nobody has an open question about a shape they depend on
